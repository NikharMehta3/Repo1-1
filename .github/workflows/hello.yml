name: Hello
on: [push]

jobs:
 build-and-run:
  runs-on: ubuntu-latest
  steps:
  - uses: actions/checkout@v1
  - name: Build Code
    run: make Repo1-1/Hello.java
  - name: run
    run: Repo1-1/Hello.java
