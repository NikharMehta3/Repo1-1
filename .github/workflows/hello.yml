name: Hello
on: [push]

jobs:
 build-and-run:
  runs-on: ubuntu-latest
  steps:
  - uses: actions/checkout@v1
  - name: Build Code
    run: make Hello.java
  - name: Enter
    run: /Repo1-1/
  - name: run
    run: ./Hello
